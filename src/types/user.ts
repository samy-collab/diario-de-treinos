// Perfil complementar da conta. Senha e credenciais continuam exclusivamente
// no Firebase Authentication e nunca sao gravadas neste documento.
export type UserProfile = {
  name: string;
  age: number;
};
